import React from "react";
import { FooterContainer, FaqsContainer, HeaderContainer } from "../containers";

const Home = () => {
	return (
		<>
			<HeaderContainer />
			<FaqsContainer />
			<FooterContainer />
		</>
	);
};

export default Home;
